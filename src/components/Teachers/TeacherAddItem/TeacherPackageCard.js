import React, { useContext } from "react"
import { Link } from "react-router-dom"
import { Button } from "react-bootstrap"

export const PackageCard = ({singleItem} ) => {
    
    return (
        <div>
            <p>{singleItem.type}</p>
          
            
        </div>
    )
}